{
  "name": "x",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "bx": "bun build --packages external packages/a/index.ts --outdir dist/bx && bun build packages/a/index.ts --outdir dist/bi"
  }
}
